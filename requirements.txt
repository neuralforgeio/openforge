# OpenForge — Python Dependencies
# ================================
#
# Requirements file for the OpenForge Python backend.
#
# Copyright (c) 2026 Dearly Febriano Irwansyah
# SPDX-License-Identifier: MIT

# FastAPI is the web framework powering the REST + SSE server (server.py).
fastapi>=0.115.0

# Uvicorn is the ASGI server that runs the FastAPI app.
uvicorn[standard]>=0.30.0

# OpenAI Python SDK — provides AsyncOpenAI for streaming chat completions
# and native function-calling (tool_calls) support.
openai>=1.50.0

# python-dotenv loads environment variables from a .env file.
python-dotenv>=1.0.1

# aiosqlite provides async access to the SQLite database used for
# conversation persistence (conversations + messages tables).
aiosqlite>=0.20.0

# websockets is used by the WebSocket endpoint for real-time streaming.
websockets>=13.0

# pydantic is used by FastAPI for request/response validation.
pydantic>=2.9.0

# rich + prompt_toolkit power the interactive TUI.
rich>=15.0.0
prompt_toolkit>=3.0.0

# tenacity provides retry/backoff for transient LLM errors.
tenacity>=9.0.0

# httpx for HTTP requests (web tools, provider health checks).
httpx>=0.27.0

# pyyaml for config file parsing.
pyyaml>=6.0

# pytest for testing (dev dependency, but included for convenience).
pytest>=8.0.0
pytest-asyncio>=0.24.0

# openpyxl — real .xlsx read/write for the spreadsheet_operations skill.
openpyxl>=3.1.0
